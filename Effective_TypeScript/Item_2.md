### TS 설정 이해하기
- `tsc --init` 명령어 실행 시 설정 파일 간단 생성, 기본적으로 `strict`모드로 설정됨
- 대부분 어디서 소스 파일을 찾을지, 어떠한 종류의 출력을 생성할지 제어하는 내용이지만, 언어 자체의 핵심 요소들을 제어하는 설정도 존재
- `noImplicitAny`(변수들이 미리 정의된 타입을 가져야 하는지) 와 `strictNullChecks`(null과 undefined가 모든 타입에서 허용되는지)를 이해해야 함